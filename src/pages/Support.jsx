import React, { useEffect } from 'react'

const Support = () => {
    useEffect(() => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }, [])
    return (
        <div>Support</div>
    )
}

export default Support